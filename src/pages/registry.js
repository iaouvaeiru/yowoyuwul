import React from 'react';
import Layout from "../components/Layout"

const Registry = () => {
    return (
        <Layout>
            <div>
                
            </div>
        </Layout>
    );
}

export default Registry;
